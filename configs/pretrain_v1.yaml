{
  "data_path": "data/clean/pretrain_data.jsonl",
  "output_dir": "models/kuroneko-v1",
  "batch_size": 1,
  "gradient_accumulation_steps": 16,
  "max_steps": 100000,
  "warmup_steps": 2000,
  "learning_rate": 3.0e-4,
  "weight_decay": 0.1,
  "grad_clip": 1.0,
  "max_length": 2048,
  "checkpoint_every": 1000,
  "use_amp": true,
  "gradient_checkpointing": true,
  "seed": 42
}
